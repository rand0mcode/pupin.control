---
classes:
  - profile::monitoring::icinga::load_test

# be a choria broker server
choria::broker::network_broker: true

mcollective::site_policies:
  - action: "allow"
    callers: "choria=robert.mcollective"
    actions: "*"
    facts: "*"
    classes: "*"
